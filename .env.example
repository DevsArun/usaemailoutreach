# ============================================
# LeadForge AI — Environment Configuration
# ============================================

# Server
NODE_ENV=production
PORT=7860
FRONTEND_URL=http://localhost:7860

# Database (PostgreSQL)
DATABASE_URL=postgres://leadforge:leadforge_secret@localhost:5432/leadforge_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=leadforge_db
DB_USER=leadforge
DB_PASSWORD=leadforge_secret

# Redis
REDIS_URL=redis://localhost:6379

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Groq AI API Keys (up to 5, rotated automatically)
GROQ_API_KEY_1=gsk_your_key_1
GROQ_API_KEY_2=gsk_your_key_2
GROQ_API_KEY_3=gsk_your_key_3
GROQ_API_KEY_4=gsk_your_key_4
GROQ_API_KEY_5=gsk_your_key_5
GROQ_MODEL=llama-3.3-70b-versatile

# Python Scraper Service
SCRAPER_SERVICE_URL=http://localhost:8000
# Max seconds the Google Maps scraper spends per campaign before returning
# whatever it has collected (prevents losing results to a hard timeout).
SCRAPE_TIME_BUDGET=480
BROWSER_POOL_SIZE=3
BROWSER_HEADLESS=true

# Email Verification (optional external provider)
EMAIL_VERIFY_PROVIDER=builtin
EMAIL_VERIFY_API_KEY=

# Logging
LOG_LEVEL=info
LOG_DIR=./logs

# Campaign Defaults
DEFAULT_DAILY_EMAIL_LIMIT=50
DEFAULT_FOLLOWUP_DELAY_1=3
DEFAULT_FOLLOWUP_DELAY_2=5
DEFAULT_FOLLOWUP_DELAY_3=7
MAX_FOLLOWUPS=3

# Name used to sign every AI-generated outreach email ("Best regards, <name>")
OUTREACH_SENDER_NAME=DevsArun
OUTREACH_SENDER_TITLE=Full Stack Developer

# Inbound Reply Polling (IMAP)
# How often (in minutes) to scan configured mailboxes for replies.
REPLY_POLL_INTERVAL_MIN=5

# Proxy (optional)
PROXY_ENABLED=false
PROXY_LIST=
